# Monday Daily Assignment - Week 8

## Usage Instructions
*   **Python Target:** Execute entirely within standard Jupyter/Python environments. Required packages include `pandas`, `statsmodels`, `matplotlib`, `scikit-learn`.
*   **Data Structure:** Ensure that all raw `.csv` datasets (including `ecommerce_sales_ts`, relatioinal `olist_*.csv` files, and `sensor.csv`) are placed within an inner **`data/`** directory. The environment dynamically recursively searches for this `data/` block. Execution bounds will inherently fail if the raw sets sit alongside the `.ipynb` notebook file.
*   **Execution:** Boot up `analysis.ipynb` and trigger a Kernel `Restart & Run All`. All plots, feature matrices splits, ACF analyses, and asymmetric cost evaluations will compile autonomously. 

## Files Details
- `analysis.ipynb`: Comprises Data Construction, Series Decompositions, Holt-Winters baseline evaluation, and the Machine ML classification rule testings inside of the Sub-steps assignment tasks.
- `observations.md`: Textually answers the required business findings requested iteratively throughout the steps.
